# This file aggregates the Terraform configuration.
# All *.tf files in this directory are automatically loaded by Terraform,
# so a dedicated main.tf is not strictly required. The resources are defined
# in the separate files (provider.tf, variables.tf, vpc.tf, iam.tf, eks.tf, outputs.tf).
# You can keep this file empty or use it to add top‑level configuration such as
# backend settings, module blocks, or additional provider configs.